
### Events Page Design Upgrade
- **3D Hero Section**: Implemented a deep radial gradient background (`#1e293b` to `#0f172a`) with `perspective: 2000px` and animated floating neon orbs for a modern "tech" vibe.
- **Vertical Timeline**: Transformed the "Programme" section into a connected vertical timeline with improved typography and spacing.
- **Interactive Speaker Cards**: Added hover effects (`transform: translateY`) and rotating gradient borders to speaker avatars.
- **Glassmorphism**: Applied `backdrop-filter: blur(12px)` to the registration call-to-action panel for a premium look.
